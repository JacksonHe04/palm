# Archived — Not Deployed

This directory contains the legacy **PALM AMS** (Admissions Management System) Django backend.

It is kept in the repository for reference only. Production no longer uses this application.

The live public website is built from [`../app/`](../app/) (Next.js).
